# Push delivery worker

Claims `web_push` notification jobs, sends generic VAPID-protected payloads, records delivery receipts, and revokes browser endpoints that return 404 or 410. It never receives or sends child, prayer, counseling, medical, custody, or safeguarding content.
