{
  "name": "@church/worker-ai-jobs",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Runs permission-filtered AI drafting against approved documents and leaves all publication decisions to humans.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@church/ai": "workspace:*",
    "@church/worker-runtime": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.23.9",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
