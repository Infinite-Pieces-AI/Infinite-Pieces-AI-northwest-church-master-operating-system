{
  "name": "@church/worker-search-console-sync",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Imports aggregate Search Console metrics into the outreach dashboard without collecting private member activity.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@church/outreach": "workspace:*",
    "@church/worker-runtime": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.23.10",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
