import Link from "next/link";
import { LoginForm } from "@/components/login-form";
export default function LoginPage(){return <section className="auth-card"><p className="auth-eyebrow">Member sign-in</p><h1>Welcome back.</h1><p>Use the email address connected to your approved member account. We will send a short-lived secure link.</p><LoginForm />{process.env.NEXT_PUBLIC_ENABLE_DEMO==="true"?<Link className="demo-entry" href="/this-week">Open the synthetic product demo →</Link>:null}<div className="auth-divider"><span>Need access?</span></div><Link className="hub-button hub-button--secondary" href="/request-access">Request member access</Link><a className="back-public" href={process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000"}>← Return to the public website</a></section>}
