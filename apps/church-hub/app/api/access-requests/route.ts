import { NextResponse, type NextRequest } from "next/server";
import { accessRequestSchema } from "@church/validation";
import { createClient } from "@supabase/supabase-js";
const buckets=new Map<string,{count:number;reset:number}>();
export async function POST(request:NextRequest){const key=request.headers.get("x-forwarded-for")?.split(",")[0]?.trim()??"unknown";const now=Date.now();const bucket=buckets.get(key);if(bucket&&bucket.reset>now&&bucket.count>=4)return NextResponse.json({message:"Too many requests. Please try again later."},{status:429});buckets.set(key,bucket&&bucket.reset>now?{...bucket,count:bucket.count+1}:{count:1,reset:now+900000});const body:unknown=await request.json().catch(()=>null);const parsed=accessRequestSchema.safeParse(body);if(!parsed.success||parsed.data.website)return NextResponse.json({message:"Please check the form and try again."},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const publishable=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!publishable)return NextResponse.json({message:"Demo mode accepted the workflow without storing personal data. Connect Supabase before production."},{status:202});const supabase=createClient(url,publishable,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}});const{error}=await supabase.rpc("submit_access_request",{p_first_name:parsed.data.firstName,p_last_name:parsed.data.lastName,p_email:parsed.data.email,p_phone:parsed.data.phone??null,p_relationship:parsed.data.relationshipToChurch,p_known_leader:parsed.data.knownLeader??null,p_reason:parsed.data.reason});if(error)return NextResponse.json({message:"The request could not be submitted right now."},{status:503});return NextResponse.json({message:"Your request is pending leader verification. Access is not automatic."},{status:202})}
